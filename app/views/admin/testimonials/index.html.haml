-content_for :sidebar do
  %h2 Manage Testimonials
  %p Testimonials are where you can post testimonials from past clients.
  %p You can change the order that testimonials appear in by clicking and dragging their title bars.
  %p You can create a new testimonial by clicking the "New Testimonial" button.
  %p You can edit a testimonial by clicking it's "Edit" link.
  %p You can delete a testimonial by clicking the red X button.

= build_link_button("New Testimonial &raquo;", new_resource_path)

%p{ :class => 'clear' }

%ul{ :id => 'position_container', :class => 'image_list' }
  - for testimonial in @testimonials
    %li{ :id => "row_#{testimonial.id}" }
      .draggable{ :style => 'height: 120px' }
        %strong= truncate(testimonial.author, :length => 20)
        = link_to(image_tag('cancel.png'), resource_path(testimonial), :class => "delete")
        %br
        = truncate("\"#{testimonial.body}\"", :length => 110)

      .actions
        = link_to('Edit', edit_resource_path(testimonial))

