:javascript
  $(document).ready(function() {
    $(".date_picker").datepicker({ dateFormat: 'MM dd, yy' });
  });

-content_for :sidebar do
  %h2 Client Gallery
  %p Enter a descriptive title for the gallery. This title will tell your client what the gallery contains. (I.e. Newborn Session)
  %p The expires on field limits how long the client can view this gallery and the photos within it. We have already assigned a default expiry, however you can change this to whatever date you like, or clear it completely if the gallery should never expire.
  %p You can optionally enter a description for the gallery. This is usually only necessary if you have more than one gallery for this client.
  %p= "Click the \"#{(controller.action_name == 'new') ? 'Create' : 'Update'}\" button when you're done."

= render "photog/shared/error_messages", :target => @gallery
= f.input :title, :hint => "Give your gallery a meaningful title. Like \"Jim & Sue's Wedding\"", :class => 'text validate[required,length[2,100]]'
= f.input :expires_on, :value => (@gallery.expires_on.blank? ? '' : @gallery.expires_on.strftime('%B %d, %Y')), :class => 'text date_picker'
%p= render :partial => "photog/shared/buttons"