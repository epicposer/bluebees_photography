#breadcrumb
  = "#{link_to('Clients', photog_path)} &raquo; #{link_to(@gallery.client.name + ' Galleries', photog_client_galleries_path(@gallery.client))} &raquo; #{link_to @gallery.title + ' Photos', collection_path} &raquo; New Photo"
  
- form_for(@photo, :url => collection_path, :html => { :multipart => true }) do |f|
  = render :partial => "form", :locals => { :f => f }
