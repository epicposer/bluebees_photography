-content_for :sidebar do
  %h2= @booking.title
  %p Here are all of the edited photos from your booking.
  %p You can click on any photo to view a larger version and scroll through the photos in a slideshow.
  %p You can leave comments for a specific photo by clicking the "Comments" link for the photo.

#breadcrumb
  = raw "#{link_to(current_client.name + ' Bookings', client_bookings_path)} &raquo; #{@booking.title} Photos"

%ul.image_list
  - for booking_photo in @booking_photos
    %li
      %div
        = truncate(booking_photo.title, :length => 22)
      = "#{link_to(image_tag(booking_photo.photo.url(:thumb), :size => '150x100'), booking_photo.photo.url, :title => booking_photo.title, :rel => 'colorbox')}"
      .actions= link_to('Comments', resource_path(booking_photo))
